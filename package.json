{
  "name": "gasoil-prices-app",
  "version": "1.0.0",
  "private": true,
  "description": "Aplicacion para consultar precios diarios de gasolina y gasoleo en Alcorcon.",
  "scripts": {
    "build": "esbuild src/main.jsx --bundle --outfile=public/app.js --format=iife --jsx=automatic --sourcemap",
    "start": "npm run build && node server.js"
  },
  "dependencies": {
    "esbuild": "^0.27.4",
    "react": "^19.2.4",
    "react-dom": "^19.2.4"
  }
}
